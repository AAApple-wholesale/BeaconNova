# 模型修改日志

## v0.4.0 - 景区运营图神经网络实验版本

日期：2026-09-11

### 新增

- 新增 `graph.py`，构建景区运营图数据集，覆盖安检通道、核心点位/险要路段、南北索道、票务全局信号和轨道交通全局信号。
- 新增 `graph_model.py`，实现基于 PyTorch 的双层图卷积网络 `ScenicGCN`，支持 CUDA 自动训练、特征标准化、目标标准化、SmoothL1 损失和早停。
- 新增 `graph_pipeline.py`，形成“全量数据读取 -> 图样本构建 -> GCN 训练 -> 舒适度/风险评分 -> 多文件导出”的完整流水线。
- 新增 `scripts/run_graph_model.py` 命令行入口，支持 epochs、batch size、hidden dim、dropout、learning rate、device 等参数。
- 新增图模型输出：`graph_nodes.csv`、`graph_edges.csv`、`graph_feature_manifest.csv`、`graph_training_history.csv`、`graph_model.pt`、`graph_run_config.csv`。
- 图结构显式包含 24 个节点、300 条有向边：9 个安检通道、8 个核心点位、2 个索道、2 个导览图节点、1 个赛题说明文档节点、票务全局节点和轨道全局节点。

### 调整

- `README.md` 增加 v0.4 图神经网络运行说明、输出文件说明和参考指标。
- `requirements.txt` 增加 `torch` 依赖。
- 包版本号更新为 `0.4.0`。
- `.gitignore` 增加图模型预测明细和冒烟测试输出忽略项，避免上传可再生成的大文件。

### 参考结果

- v0.4.0 图模型使用全部已给数据，节点特征 97 维，训练设备为 CUDA。
- 最近 21 天时序测试集：
  - 30 分钟通过人数 MAE：2.8913，RMSE：3.4760，WAPE：0.3265。
  - 30 分钟等待时间 MAE：0.5349，RMSE：0.6392，WAPE：0.3699。
  - 60 分钟通过人数 MAE：3.0232，RMSE：3.5979，WAPE：0.3410。
  - 60 分钟等待时间 MAE：0.5389，RMSE：0.6377，WAPE：0.3723。
- 相比 v0.3.0 表格容量基线，四个预测目标均明显降低误差，尤其通过人数预测 MAE 从约 4.13/4.17 降至约 2.89/3.02。

### 当前限制

- 当前图拓扑基于赛题文字、点位名称和业务常识手工构建，尚未从导览图中自动抽取距离、坡度和游线权重。
- 图卷积使用同一静态邻接矩阵，尚未学习节假日、高峰期、设备状态变化下的动态边权。
- 图模型仍依赖已有滞后/滚动特征，没有显式使用 TCN/LSTM/Transformer 建模长时间序列记忆。
- 天气数据仍未接入真实外部源，天气暴露风险来自设施文本和容量参数。

### 下一版本计划

- v0.5.0：加入时间注意力或 TCN 模块，形成时空图神经网络。
- v0.6.0：基于导览图和历史时滞相关性校准图边权。
- v0.7.0：增加干预策略仿真与成本收益评估。
## v0.3.0 - 容量约束与索道压力版本

日期：2026-09-11

### 新增

- 新增 `FACILITY_FILE` 与 `ROPEWAY_FILE` 配置，接入核心点位容量表和南北索道运营参数表。
- 新增 `load_facility_capacity()`，读取点位有效面积、瞬时最大容纳量、小时极限通行量、合理停留时长、设施配置与风险描述。
- 新增 `load_ropeway_capacity()`，读取索道额定载客、发车间隔、理论运力、饱和运力、平台容量和合理排队时长上限。
- 新增点位天气暴露指数、瓶颈风险指数、服务速率、容量占比等设施画像字段。
- 新增索道运营折减系数、平台承压、轨道到达对索道运力压力等特征。
- 新增 `capacity_profile.csv` 输出，用于记录本次模型使用的容量与索道业务参数。
- 新增风险主导因素输出，区分等待压力、客流压力、包检压力、增长压力、容量压力、索道压力和暴露风险。
- 新增 `--skip-facility` 参数，支持跳过容量/索道特征做消融测试。
- 新增 `requirements.txt`，补充 `xlrd` 依赖以读取 `.xls` 数据表。

### 调整

- `build_feature_frame()` 增加设施容量和索道参数输入。
- 风险评分从“等待 + 客流 + 包检 + 增长”升级为“等待 + 客流 + 包检 + 增长 + 容量 + 索道 + 暴露风险”的综合评分。
- 修正风险等级中文乱码，输出统一为蓝色、黄色、橙色、红色。
- README 更新为 v0.3 数据链路、依赖、输出文件和参考指标。

### 参考结果

- v0.3.0 完整链路使用 107 个数值特征。
- 最近 21 天时序测试集：
  - 30 分钟通过人数 MAE：4.1286，RMSE：4.8800，WAPE：0.4628。
  - 30 分钟等待时间 MAE：0.6204，RMSE：0.7375，WAPE：0.4282。
  - 60 分钟通过人数 MAE：4.1697，RMSE：4.9179，WAPE：0.4657。
  - 60 分钟等待时间 MAE：0.6350，RMSE：0.7515，WAPE：0.4368。
- 相比 v0.2.0，30 分钟等待时间 MAE 从约 0.6289 降至 0.6204，30 分钟通过人数 MAE 从约 4.1400 降至 4.1286。

### 当前限制

- 点位容量和索道参数目前以景区全局压力形式投影到安检口，尚未完成安检口到具体游线/点位的空间映射。
- 天气暴露指数目前基于设施文本规则计算，尚未接入真实天气和体感温度数据。
- 风险权重仍为启发式配置，后续需要结合专家阈值、容量校准和业务复盘进一步标定。
- 当前模型仍为表格基线模型，尚未使用 PyTorch 时空图模型。

### 下一版本计划

- v0.4.0：建立安检口、索道、点位之间的游线拓扑映射，形成节点级风险传播特征。
- v0.5.0：接入天气体感因子与节假日外部日历配置。
- v0.6.0：增加策略仿真与干预收益评估。
## v0.2.0 - 票务聚合与日历特征版本

日期：2026-09-11

### 新增

- 新增 `TICKET_FILE` 配置，接入赛题票务流水表。
- 新增 `load_ticket_aggregates()`，按块读取 500MB 级票务大表并聚合为 5 分钟粒度特征。
- 新增票务入园量、近 30/60 分钟入园量、当日累计入园量。
- 新增票种占比、票务通道占比、季节标签占比等客群和来源结构特征。
- 新增日历特征，包括国庆假期、调休工作日、有效周末、暑期、假期序位、距假期天数。
- 新增 `--skip-ticket` 命令行参数，便于快速跳过票务大表进行冒烟测试。
- 新增 `feature_manifest.csv` 输出，用于记录本次训练使用的特征清单。

### 调整

- `build_feature_frame()` 增加可选票务特征输入。
- `run_baseline()` 默认启用票务聚合特征。
- README 更新为 v0.2 数据链路与运行说明。

### 当前限制

- 日历特征目前内置 2025 年国庆/中秋假期与调休日，后续应替换为外部日历配置文件。
- 票务特征为景区整体时间窗聚合，尚未映射到不同安检口和园内节点。
- 尚未接入天气、点位容量 XLS 和导览图拓扑。
- 当前风险阈值仍为启发式规则，后续需要结合容量参数和业务专家意见校准。
- 当前模型仍为表格基线模型，尚未使用 PyTorch 时空图模型。

### 下一版本计划

- v0.3.0：加入天气体感因子与舒适度分项解释。
- v0.4.0：接入点位容量表与索道参数，形成节点级风险阈值。
- v0.5.0：增加策略仿真与干预收益评估。

## v0.1.0 - 初版基线模型

日期：2026-09-11

### 新增

- 建立 `src/beaconnova/` Python 包结构。
- 新增赛题安检口数据和轨道交通数据读取模块。
- 新增 5 分钟粒度时间特征、通道滞后特征、滚动统计特征和轨道到达冲击特征。
- 新增未来 30 分钟、60 分钟通过人数和平均等待时间预测流程。
- 新增基于 `HistGradientBoostingRegressor` 的可解释基线模型。
- 新增历史中位数兜底模型，保证缺少部分 sklearn 能力时仍可运行。
- 新增舒适度评分函数，输出 0-100 分体验分。
- 新增蓝、黄、橙、红四级风险规则，采用固定业务阈值与预测分位阈值结合的初版策略。
- 新增 `scripts/run_baseline.py` 命令行入口。
- 新增 `README.md` 说明运行方式、输出文件和后续计划。



