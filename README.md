# BeaconNova 基线模型

本仓库用于北京数智创新大赛赛题五“基于多源数据融合的文旅场景舒适度评估与风险超前预警模型与分析”。当前版本已推进到 v0.8.0：在 v0.5.0 真实天气增强 GCN 基础上，完成策略仿真、历史拥堵传导边权校准、TCN-GCN 时序图预测模型、本地可视化看板，并进一步引入自适应邻接、门控膨胀 TCN 和验证集后校准，使优化版 GCN/TCN-GCN 的 MAE 均低于 v0.5.0。模型链路已覆盖赛题数据、真实天气、多源图结构、提前预警、干预收益评估和展示交付。
## 项目目标

模型面向八达岭长城景区安检口、票务入园、轨道交通到达、核心点位承载能力和索道运力数据，完成以下任务：

1. 读取赛题配套数据中的安检口 5 分钟客流、票务入园流水、轨道交通客流、点位容量和索道参数。
2. 构建时间特征、日历特征、票务聚合特征、通道历史特征、轨道到达冲击特征、点位容量压力和索道运力压力。
3. 预测未来 30 分钟和 60 分钟的通道人数与平均排队等待时间。
4. 根据预测客流、等待时间、包检压力、容量压力、索道压力和天气暴露风险计算舒适度分数。
5. 输出蓝、黄、橙、红四级风险预警，并给出主导风险因素。
6. 通过 `scripts\\fetch_weather.py` 从 Open-Meteo 拉取八达岭真实历史天气，生成 `weather.csv`。

## 目录结构

```text
.
├─ src/
│  └─ beaconnova/
│     ├─ config.py
│     ├─ data.py
│     ├─ features.py
│     ├─ scoring.py
│     ├─ baseline.py
│     ├─ graph.py
│     ├─ graph_model.py
│     ├─ graph_pipeline.py
│     ├─ topology.py
│     ├─ temporal_graph_model.py
│     ├─ temporal_graph_pipeline.py
│     ├─ strategy.py
│     ├─ weather_client.py
│     └─ pipeline.py
├─ scripts/
│  ├─ fetch_weather.py
│  ├─ run_baseline.py
│  ├─ run_graph_model.py
│  ├─ run_temporal_graph_model.py
│  ├─ run_strategy_simulation.py
│  └─ build_dashboard_data.py
├─ dashboard/
│  ├─ index.html
│  ├─ styles.css
│  ├─ app.js
│  └─ assets/
├─ outputs/
├─ requirements.txt
├─ README.md
└─ MODEL_CHANGELOG.md
```
## 环境要求

建议使用 Python 3.10 及以上版本。最低依赖如下：

```text
pandas
numpy
scikit-learn
xlrd
torch
```

其中 `xlrd` 用于读取赛题提供的 `.xls` 容量表和索道表。当前代码不强制使用 GPU，便于先在普通 CPU 环境验证数据链路；后续接入 PyTorch 时空图模型时再启用 GPU。

## 快速运行

在项目根目录执行完整基线，包括票务大表、点位容量和索道参数：

```powershell
python scripts\run_baseline.py
```

如果只是快速冒烟测试，可跳过票务大表：

```powershell
python scripts\run_baseline.py --skip-ticket
```

也可以跳过容量/索道特征，用于和旧版做消融对比：

```powershell
python scripts\run_baseline.py --skip-facility
```

显式指定数据目录和输出目录：

```powershell
python scripts\run_baseline.py `
  --data-dir "赛题五基于多源数据融合的文旅场景舒适度评估与风险超前预警模型与分析0805" `
  --output-dir outputs\baseline_v0_3
```

## 输出文件

运行完成后，默认在 `outputs/baseline/` 下生成：

| 文件 | 说明 |
|---|---|
| `predictions.csv` | 未来 30/60 分钟客流和等待时间预测、舒适度、风险等级、风险主因和风险分项 |
| `metrics.csv` | 30/60 分钟预测误差，包括 MAE、RMSE、WAPE |
| `risk_summary.csv` | 不同预测窗口下各风险等级数量和主导风险因素数量 |
| `feature_manifest.csv` | 本次训练使用的数值特征清单 |
| `capacity_profile.csv` | 点位容量、服务效率、索道运力和暴露风险画像 |

## 模型说明

### 数据读取

当前使用五类数据：

- `表2安检口每5分钟安检数据：security_gate.csv.csv`
  - 核心建模表，包含安检口、通道、通过人数、平均等待时间和包检量。
- `表1入园票务数据表：ticket.csv`
  - 按块读取并聚合为 5 分钟入园量、票种占比、票务通道占比和季节标签占比。
- `表3轨道交通客流表：rail_flow.csv`
  - 入园客流先行信号，按 5 分钟时间窗聚合并构造近 30/60 分钟到达冲击特征。
- `八达岭景区核心打卡点位全量承载与服务效率参数总表.xls`
  - 提取核心点位瞬时容量、小时通行能力、停留时长、天气防护和瓶颈风险。
- `南北索道基本情况.xls`
  - 提取索道饱和运力、入口平台容量、合理排队上限和运营折减系数。

### 特征工程

主要特征包括：

- 时间特征：小时、分钟、星期、月份、周末、日内 5 分钟序号、正余弦周期编码。
- 日历特征：国庆假期、调休工作日、有效周末、暑期、假期序位、距假期天数。
- 票务特征：当前 5 分钟入园量、近 30/60 分钟入园量、当日累计入园量、票种/通道/季节标签占比。
- 通道历史特征：人数、等待时间、包检量的 1/2/3/6/12 个窗口滞后。
- 滚动特征：15/30/60 分钟滚动均值、最大值。
- 安检口聚合特征：同安检口总通过量、平均等待、通道负载占比、全系统安检压力。
- 轨道到达特征：当前 5 分钟、近 30 分钟、近 60 分钟轨道客流量。
- 容量与索道特征：点位瞬时容量压力、小时通行压力、瓶颈压力、票务对容量压力、轨道对索道压力、索道平台压力、天气暴露风险。

### 预测模型

当前采用 `HistGradientBoostingRegressor` 作为基线回归模型，分别预测：

- 未来 30 分钟通过人数
- 未来 60 分钟通过人数
- 未来 30 分钟平均等待时间
- 未来 60 分钟平均等待时间

若运行环境缺少对应 sklearn 模型，则自动退化为历史中位数预测器，保证流程可跑通。

### 舒适度与风险等级

舒适度分数为 0-100 分，分数越高表示体验越好。v0.3.0 综合考虑等待压力、预测客流压力、包检压力、短时增长压力、容量压力、索道压力和天气暴露风险。风险等级采用“固定业务阈值 + 本次预测分位阈值”的混合规则，既保留绝对风险含义，也保证在低负载样本中具备排序能力。

| 等级 | 含义 |
|---|---|
| 蓝色 | 轻度关注，舒适度开始下降或负载上升 |
| 黄色 | 明显排队风险，需要提前观察和分流 |
| 橙色 | 高负载或等待时间快速上升，需要启动干预 |
| 红色 | 严重拥堵风险，需要立即处置 |

## v0.3.0 参考结果

在完整数据链路下，v0.3.0 使用 107 个数值特征。最近 21 天时序测试集结果如下：

| 目标 | 预测窗口 | MAE | RMSE | WAPE |
|---|---:|---:|---:|---:|
| 通过人数 | 30 分钟 | 4.1286 | 4.8800 | 0.4628 |
| 等待时间 | 30 分钟 | 0.6204 | 0.7375 | 0.4282 |
| 通过人数 | 60 分钟 | 4.1697 | 4.9179 | 0.4657 |
| 等待时间 | 60 分钟 | 0.6350 | 0.7515 | 0.4368 |

## 图神经网络实验 v0.4.0

v0.4.0 新增 PyTorch 图卷积模型，作为表格基线之外的时空传播建模尝试。图结构包含 24 个节点：9 个安检通道节点、8 个核心点位/险要路段节点、2 个索道节点、2 个导览图上下文节点、1 个赛题说明文档节点、1 个票务全局节点、1 个轨道交通全局节点。边关系包括同安检口通道连接、票务/轨道全局信号连接、前山/外餐入口至南北游线连接、索道至平台/打卡点连接，以及核心点位沿游线的顺序连接。

运行图模型：

```powershell
python scripts\run_graph_model.py `
  --data-dir "赛题五基于多源数据融合的文旅场景舒适度评估与风险超前预警模型与分析0805" `
  --output-dir outputs\graph_v0_4
```

图模型输出文件：

| 文件 | 说明 |
|---|---|
| `graph_predictions.csv` | 图模型预测、舒适度、风险等级和风险主因明细 |
| `graph_metrics.csv` | 图模型 30/60 分钟预测误差 |
| `graph_risk_summary.csv` | 图模型风险等级和主导风险因素汇总 |
| `graph_nodes.csv` | 景区运营图节点清单 |
| `graph_edges.csv` | 景区运营图边关系清单 |
| `graph_feature_manifest.csv` | 图模型节点特征清单 |
| `graph_training_history.csv` | 训练与验证损失曲线 |
| `graph_model.pt` | 本次训练得到的 PyTorch 权重与标准化参数 |
| `context_assets.csv` | 导览图与赛题说明文档的静态上下文画像 |

v0.4.0 使用全部已给数据，其中文旅业务表格进入动态特征，导览图和赛题说明文档进入静态上下文节点；训练设备为 CUDA。最近 21 天时序测试集结果如下：

| 目标 | 预测窗口 | MAE | RMSE | WAPE |
|---|---:|---:|---:|---:|
| 通过人数 | 30 分钟 | 2.8913 | 3.4760 | 0.3265 |
| 等待时间 | 30 分钟 | 0.5349 | 0.6392 | 0.3699 |
| 通过人数 | 60 分钟 | 3.0232 | 3.5979 | 0.3410 |
| 等待时间 | 60 分钟 | 0.5389 | 0.6377 | 0.3723 |

## 真实天气接口 v0.5.0

v0.5.0 新增 Open-Meteo Historical Weather API 接口，默认使用八达岭长城附近坐标 `40.3597, 116.0201`，自动按照安检数据日期范围拉取小时级历史天气。接口免密，输出文件为赛题数据目录下的 `weather.csv`。

```powershell
python scripts\fetch_weather.py `
  --data-dir "赛题五基于多源数据融合的文旅场景舒适度评估与风险超前预警模型与分析0805" `
  --print-url
```

天气字段包括气温、体感温度、相对湿度、降水量、降雨标记、风速、阵风、天气代码、云量、热压力、冷压力、风压力和综合天气舒适惩罚。若目录中不存在 `weather.csv`，代码会退化为可替换的季节性天气代理特征；一旦真实文件存在，训练和评分自动优先使用真实天气。

v0.5.0 天气增强 GCN 使用 112 个节点特征。最近 21 天时序测试集结果如下：

| 目标 | 预测窗口 | MAE | RMSE | WAPE |
|---|---:|---:|---:|---:|
| 通过人数 | 30 分钟 | 2.6125 | 3.1809 | 0.2950 |
| 等待时间 | 30 分钟 | 0.4693 | 0.5660 | 0.3245 |
| 通过人数 | 60 分钟 | 2.6054 | 3.1351 | 0.2939 |
| 等待时间 | 60 分钟 | 0.4619 | 0.5579 | 0.3191 |
## 策略仿真与边权校准 v0.6.0/v0.8.0

v0.6.0 完成了“从预警到处置”的第一版闭环。`src/beaconnova/strategy.py` 会读取预测结果，对黄色及以上风险样本自动匹配增开/预启备用安检通道、低负载游线分流、索道入口削峰与批次放行、天气保障服务点前置等策略，并估算风险分数下降、舒适度提升、等待时间压降和橙红预警压降。

```powershell
python scripts\run_strategy_simulation.py `
  --prediction-path outputs\graph_v0_5_weather\graph_predictions.csv `
  --output-dir outputs\strategy_v0_6 `
  --horizon 12
```

同时新增 `src/beaconnova/topology.py`，将手工业务图升级为“业务先验 + 历史时滞相关性”的可校准拓扑。系统会在 0-60 分钟时滞窗口内计算拥堵传导相关性，并输出 `graph_edge_weights.csv`，保留每条边的先验权重、最佳时滞、历史相关性和最终校准权重。

v0.8.0 优化版自适应 GCN 使用 112 个节点特征、24 个节点、300 条有向边，在校准边权基础上加入可学习自适应邻接和验证集 Ridge 后校准。最近 21 天时序测试集结果如下：

| 目标 | 预测窗口 | MAE | RMSE | WAPE |
|---|---:|---:|---:|---:|
| 通过人数 | 30 分钟 | 2.5884 | 3.1598 | 0.2923 |
| 等待时间 | 30 分钟 | 0.4627 | 0.5617 | 0.3199 |
| 通过人数 | 60 分钟 | 2.5352 | 3.0535 | 0.2860 |
| 等待时间 | 60 分钟 | 0.4540 | 0.5487 | 0.3137 |

策略仿真在 60 分钟窗口下触发 4509 次分流引导策略，平均风险分数下降 0.0602，平均舒适度提升 6.02 分，平均等待时间压降 0.21 分钟，橙红预警样本从 1804 条降至 451 条。

## 时序图模型与可视化看板 v0.7.0/v0.8.0

v0.7.0 新增 `src/beaconnova/temporal_graph_model.py` 和 `src/beaconnova/temporal_graph_pipeline.py`，形成 TCN-GCN 时序图模型。模型先读取每个节点过去 12 个 5 分钟窗口，即过去 60 分钟的多源序列，用 TCN 提取时间模式，再通过校准后的图卷积传播安检口、票务、轨道、索道、核心点位和天气上下文之间的风险影响，输出未来 30/60 分钟预测与风险预警。

```powershell
python scripts\run_temporal_graph_model.py `
  --data-dir "赛题五基于多源数据融合的文旅场景舒适度评估与风险超前预警模型与分析0805" `
  --output-dir outputs\temporal_graph_v0_6 `
  --sequence-length 12 `
  --epochs 18 `
  --batch-size 384
```

v0.8.0 优化版 TCN-GCN 将原始时序层升级为门控膨胀残差 TCN，并同样加入可学习自适应邻接和验证集后校准。最近 21 天测试集结果如下：

| 目标 | 预测窗口 | MAE | RMSE | WAPE |
|---|---:|---:|---:|---:|
| 通过人数 | 30 分钟 | 2.6027 | 3.1527 | 0.2939 |
| 等待时间 | 30 分钟 | 0.4639 | 0.5602 | 0.3207 |
| 通过人数 | 60 分钟 | 2.5532 | 3.0838 | 0.2886 |
| 等待时间 | 60 分钟 | 0.4553 | 0.5501 | 0.3149 |

新增 `dashboard/` 本地看板，汇总模型指标、地图热区、60 分钟风险等级、真实天气体感、策略干预收益和历史传导校准边权。先生成看板数据，再打开 HTML 文件即可查看：

```powershell
python scripts\build_dashboard_data.py --project-root .
start dashboard\index.html
```

## 优化依据与后续计划完成情况

README 旧版列出的四项后续计划已全部完成。本轮优化参考了 STGCN 的时空图联合建模、Graph WaveNet 的自适应邻接矩阵和膨胀时间卷积、TCN 的残差膨胀卷积稳定训练思路，并额外加入只使用验证集的预测后校准。

1. 策略仿真模块已完成，覆盖增开通道、分流引导、索道削峰和天气保障服务点前置。
2. 图拓扑已升级为可校准边权，融合业务先验和历史拥堵时滞相关性。
3. 图卷积已加入 TCN 时间序列模块，形成可训练、可复现的时空图预测模型。
4. 可视化看板已建立，可展示地图热力、风险等级、天气体感和干预收益。

下一步可继续做三件事：一是接入实时天气预报和景区实时闸机流；二是把策略仿真从启发式收益升级为约束优化；三是用更多节假日或真实运营复盘数据校准风险阈值。

